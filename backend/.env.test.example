SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=your-local-anon-key

# Both values print from `supabase start` (API_URL, PUBLISHABLE_KEY).
# These are the same fixed local-dev defaults for every Supabase CLI
# project, not secrets — safe to keep in .env.test.
